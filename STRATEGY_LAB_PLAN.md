# Swing Lab Strategy Lab

## 목적
라이브 전략의 임계값을 종목 하나의 결과를 보고 즉흥적으로 조정하지 않는다. 현재 라이브 로직을 기준선(baseline)으로 동결하고, 후보 전략/필터를 동일 데이터와 동일 비용 가정에서 경쟁시킨 뒤 OOS(out-of-sample)에서 살아남은 변경만 승격한다.

## 1. 검증 단위
종목별 백테스트는 참고자료다. 전략의 채택/폐기는 메인 유니버스 전체의 pooled 결과로 판단한다.

필수 집계:
- 전체 기간
- 최근 2년
- 종목별 분포
- 섹터별
- 시장 regime별: 상승 / 중립 / 약세
- 변동성 regime별
- IS / OOS / walk-forward

## 2. 핵심 지표
- 거래 수
- 승률
- 평균/중앙 거래수익률
- 총수익률
- Profit Factor
- 최대낙폭(MDD)
- Sharpe (가능한 경우)
- 평균 보유기간
- 기대값 = 승률×평균이익 - 패율×평균손실
- 비용/슬리피지 적용 후 결과

## 3. 현재 전략 baseline
- confirmed_pullback: 확인형 눌림반등
- rsi2_trend_reversion: RSI2 추세내 과매도
- momentum_pullback: 모멘텀 눌림 지속
- volatility_breakout: 실험/숨김 유지

라이브 임계값은 연구 결과가 나오기 전까지 임의 변경 금지.

## 4. 후보 실험
각 변경은 한 번에 하나씩 추가해 기여도를 측정한다.

### A. 단순화
복잡한 확인 조건을 줄인 버전과 baseline 비교.

### B. Relative Strength
- 20일 종목 수익률 - SPY 20일 수익률
- 60일 종목 수익률 - SPY 60일 수익률
- 가능하면 섹터 ETF 대비 RS도 별도 실험
RS는 처음부터 하드필터로 확정하지 않고 후보 변수로 검증한다.

### C. 수급 품질
현재 상대거래량/5일대20일/반전거래량/상승하락 거래량 비율에 다음 후보를 독립 실험:
- OBV slope
- CMF(20)
- 대량거래 + 장대음봉 패널티
- 대량거래 + 긴 윗꼬리 패널티
- 반전 양봉의 거래량 확인

### D. 시장 regime
SPY/QQQ 장기 추세 및 시장 변동성 상태에 따라 전략 성과를 분리한다. 시장 필터가 실제 OOS 기대값을 개선하는 경우에만 채택한다.

## 5. 백테스트 사용 원칙
- 특정 종목 10년 거래 수가 작으면 전략 폐기의 근거로 사용하지 않는다.
- 개별 종목 백테스트는 경고/참고 정보로 표시한다.
- 전략 전체 pooled OOS가 지속적으로 음의 기대값이면 해당 전략을 라이브에서 퇴출/연구 상태로 내린다.
- 단순히 승률만 최적화하지 않는다. PF, 기대값, MDD, 거래 수를 함께 본다.
- 테스트한 파라미터가 많아질수록 과최적화 위험을 기록한다.

## 6. 승격 규칙 초안
정확한 컷오프는 baseline 분포를 먼저 측정한 뒤 확정한다. 연구 전에 PF 1.2, 승률 60% 같은 임의 숫자를 하드코딩하지 않는다.

승격 후보는 최소한:
1. OOS 기대값 > 0
2. OOS PF > 1
3. 최근 구간에서도 방향성이 붕괴하지 않음
4. 충분한 pooled 표본
5. 특정 소수 종목/섹터가 수익 대부분을 만들지 않음
6. 비용/슬리피지 적용 후에도 우위 유지
7. baseline 대비 실질 개선

## 7. 라이브 파이프라인 목표
데이터 무결성 → 메인 유니버스 → 전략 신호 → 시장/RS/수급 품질(검증된 필터만) → ATR 기반 trade plan → S 후보 → 후보에 한해 뉴스/공시 위험검사 → 추천 기록

## 8. 금지사항
- 한 종목의 실패를 보고 즉시 RSI/이평선 임계값 변경
- 결과가 좋아질 때까지 파라미터 반복 탐색 후 같은 데이터로 성능 주장
- 백테스트 승률을 라이브 신호 점수로 그대로 사용
- 뉴스/LLM 판단을 가격 데이터 검증보다 먼저 실행
- 개별 종목의 작은 표본을 전략 전체 통계로 해석

## 다음 구현 순서
1. Strategy Lab pooled backtester
2. baseline 3전략 전체 유니버스 결과표
3. walk-forward/OOS 분리
4. RS 실험군
5. OBV/CMF 및 candle-volume quality 실험군
6. baseline과 비교표 생성
7. 승격된 필터만 라이브 엔진에 반영
