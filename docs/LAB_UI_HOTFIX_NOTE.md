# Automatic Research Lab UI hotfix

The lab must render its baseline and A/B structure even when there are zero trades. The lab renderer owns the lab tab and restores the A/B view if a legacy renderer overwrites the shared host.