# Status

Branch `fix/lab-empty-render-race` contains the A/B lab renderer ownership fix. It is intended to be merged into `main` so the production lab does not appear blank when there are zero trades.
