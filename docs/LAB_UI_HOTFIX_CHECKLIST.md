# Lab UI hotfix checklist

- The lab tab must show a visible structure with zero trades.
- The A/B lab renderer must remain visible after navigation.
- The baseline strip and both track cards must render before any trade exists.
- Legacy single-lab rendering must not overwrite the A/B view.
